<mah:MetroWindow x:Class="MusicSpot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:local="clr-namespace:MusicSpot"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:views="clr-namespace:MusicSpot.Views"
        mc:Ignorable="d"
        Title="MusicSpot" Height="800" Width="1000" MinHeight="600" MinWidth="800"
                 TitleCaps="False">

    <Grid ShowGridLines="True" Margin="0,0,0,-3">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="5*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <!--MetroTabs for application mode switching - music, videos, pictures-->

        <Controls:MetroAnimatedTabControl
            Controls:TabControlHelper.IsUnderlined="True"
            Controls:TabControlHelper.Transition="Default"
            Grid.Column="1"
            SelectedIndex="0">
            <Controls:MetroTabItem Header="Music">
                <views:MusicView DataContext="{Binding MusicViewModel}"/>
            </Controls:MetroTabItem>
            <Controls:MetroTabItem Header="Video">
                <Grid Column="0">
                    <Label Foreground="Aqua" Background="Cyan">Hello there</Label>
                </Grid>
                <!--<views:VideoView />-->
            </Controls:MetroTabItem>
            <Controls:MetroTabItem Header="Picture">
                <views:PictureView/>
            </Controls:MetroTabItem>

        </Controls:MetroAnimatedTabControl>
    </Grid>
</mah:MetroWindow>
